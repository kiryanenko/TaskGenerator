<h1 class="page-header">
  <table width="100%"><tr>
    <td>Ваши билеты:</td>
    <td width="220px" align="right">
      <a href="<%= new_question_card_path %>" type="button" class="btn btn-success"><i class="fa fa-plus"></i> Создать новый</a>
    </td>
  </tr></table>
</h1>

<% if @question_cards.size == 0 %>
    <p>Вы еще не добавили ни одной задачи. Сделать это вы можете <%= link_to 'здесь', new_task_path %></p>
<% else %>
    <div class="list-group">
      <% @question_cards.each do |card| %>
          <a href="/tasks/<%= card.id %>" class="list-group-item list-group-item-action">
            <span class="pull-right text-muted small"><em><%= card.updated_at.to_formatted_s(:short) %></em></span>
            <h4 class="list-group-item-heading"><%= card.title %></h4>
            <p class="list-group-item-text"><%= card.description %></p>
          </a>
      <% end %>
    </div>

<% end %>